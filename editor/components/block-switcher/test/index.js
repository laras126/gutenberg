/**
 * Internal dependencies
 */
import { BlockSwitcher } from '../';

describe( 'BlockSwitcher', () => {
	test( 'sample test', () => {
		// Just a placeholder.
		expect( true ).toBe( true );
	} );
} );
